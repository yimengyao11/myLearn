export const Biji = () => {
  return (
    <div>
      {/* 需求：从后端拿到数据，以表格形式展示在UI */}
      {/* 一、做一个静态的表格页面 
1，去antdesign找符合需求的Table模板
---找到模板后，看哪些代码是是业务需要的，比如列组件，数据组件，传值等，复制到自己的代码里面 */}
      {/* 二、获取数据
1 */}
    </div>
  );
};
